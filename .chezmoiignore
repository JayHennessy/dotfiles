README.md
install.sh
LICENSE
aws-ssh-plan.md
.gemini_api_key

# Yazi rewrites package.toml itself (hashes, formatting) whenever the
# yazi-plugins script runs, which would perpetually drift against chezmoi.
# The script's `ya pkg add` reproduces the plugin list, so let yazi own this.
.config/yazi/package.toml

# AWS
.aws/cli/
.aws/sso/
.aws/old-config

# Kube
.kube/cache/
.kube/gke_gcloud_auth_plugin_cache
.kube/kubectx

# Claude Code (transient/generated files)
.claude/.credentials.json
.claude/backups/
.claude/cache/
.claude/debug/
.claude/history.jsonl
.claude/mcp-needs-auth-cache.json
.claude/plugins/blocklist.json
.claude/plugins/known_marketplaces.json
.claude/plugins/cache/
.claude/projects/
.claude/shell-snapshots/
.claude/stats-cache.json
.claude/telemetry/
.claude/todos/

# Dotfiles-repo project instructions for Claude Code — do not deploy to ~
CLAUDE.md
